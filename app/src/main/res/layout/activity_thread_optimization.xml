<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/ivBackground"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="背景"
        android:scaleType="centerCrop" />

    <LinearLayout
        android:id="@+id/rootLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingLeft="16dp"
        android:paddingTop="8dp"
        android:paddingRight="16dp"
        android:paddingBottom="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="线程优化"
            android:textColor="@color/hf_text_primary"
            android:textSize="24sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/btnBack"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:background="@drawable/bg_button"
            android:backgroundTint="@null"
            android:text="返回"
            android:textAllCaps="false"
            android:textColor="@color/hf_text_primary" />
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvStatus"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_panel"
                android:padding="12dp"
                android:text="正在获取状态..."
                android:textColor="@color/hf_text_primary"
                android:textSize="13sp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:text="AppOpt 线程优化"
                android:textColor="@color/hf_text_primary"
                android:textSize="18sp"
                android:textStyle="bold" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:lineSpacingExtra="2dp"
                android:text="基于 AppOpt 编译二进制的线程亲和优化。自动适配 6+2 / 3+4+1 / 2+3+2+1 / 4+4 / 4+3+1 等核心拓扑。"
                android:textColor="@color/hf_text_secondary"
                android:textSize="12sp" />

            <Switch
                android:id="@+id/switchAppOpt"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:checked="false"
                android:text="启动 AppOpt 线程优化"
                android:textColor="@color/hf_text_primary"
                android:textSize="16sp" />

            <Switch
                android:id="@+id/switchAppOptCpuControl"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:checked="true"
                android:text="同时启用系统级 CPU 优化(cpu_control)"
                android:textColor="@color/hf_text_primary"
                android:textSize="14sp" />

            <Button
                android:id="@+id/btnEditRules"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:layout_marginTop="8dp"
                android:background="@drawable/bg_button"
                android:backgroundTint="@null"
                android:text="编辑线程规则"
                android:textAllCaps="false"
                android:textColor="@color/hf_text_primary" />

            <Button
                android:id="@+id/btnAppOptLog"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:layout_marginTop="8dp"
                android:background="@drawable/bg_button"
                android:backgroundTint="@null"
                android:text="查看 AppOpt 日志"
                android:textAllCaps="false"
                android:textColor="@color/hf_text_primary" />

            <Button
                android:id="@+id/btnAppBinding"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:layout_marginTop="8dp"
                android:background="@drawable/bg_button"
                android:backgroundTint="@null"
                android:text="应用线程绑定（按 APP 配置）"
                android:textAllCaps="false"
                android:textColor="@color/hf_text_primary" />

        </LinearLayout>
    </ScrollView>
</LinearLayout>
</FrameLayout>